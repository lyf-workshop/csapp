# 复制为 .env 并改成你自己的强密码（登录 code-server 用）
# cp .env.example .env
CSAPP_PASSWORD=换成一个足够强的密码

# GitHub 下载加速代理前缀（国内构建用）。默认 gh-proxy.com。
# 若该代理失效，可换成 https://ghfast.top/ 或 https://ghproxy.net/ 等；
# 网络能直连 GitHub 时可留空。
# GH_PROXY=https://gh-proxy.com/
